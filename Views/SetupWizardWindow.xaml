<Window x:Class="DisplayProfileManager.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Setup" Height="520" Width="560"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        Background="{DynamicResource BgBrush}"
        Foreground="{DynamicResource TextBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Background="{DynamicResource BgBrush}">
        <Grid Margin="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock x:Name="TxtTitle" Text="Welcome" FontSize="22" FontWeight="SemiBold"/>
                <TextBlock x:Name="TxtSub" Text="Quick setup" Foreground="{DynamicResource MutedBrush}" Margin="0,6,0,0" TextWrapping="Wrap"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" Margin="0,18,0,12" VerticalScrollBarVisibility="Hidden">
                <StackPanel>
                    <TextBlock x:Name="LblLang" Text="Language" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <ComboBox x:Name="CmbLang" SelectionChanged="Lang_Changed">
                        <ComboBoxItem Content="English" Tag="en"/>
                        <ComboBoxItem Content="Русский" Tag="ru"/>
                    </ComboBox>

                    <TextBlock x:Name="LblTheme" Text="Theme" FontWeight="SemiBold" Margin="0,16,0,6"/>
                    <ComboBox x:Name="CmbTheme" SelectionChanged="Theme_Changed">
                        <ComboBoxItem Content="Dark" Tag="dark"/>
                        <ComboBoxItem Content="Light" Tag="light"/>
                        <ComboBoxItem Content="Custom" Tag="custom"/>
                    </ComboBox>
                    <Button x:Name="BtnEditPalette" Content="Edit palette…" Style="{StaticResource GhostButton}"
                            HorizontalAlignment="Left" Margin="0,8,0,0" Click="EditPalette_Click" Visibility="Collapsed"/>

                    <TextBlock x:Name="LblMonitor" Text="Monitor" FontWeight="SemiBold" Margin="0,16,0,6"/>
                    <ComboBox x:Name="CmbMonitor"/>

                    <Separator Margin="0,16"/>
                    <CheckBox x:Name="ChkAutostart" Content="Start with Windows" IsChecked="True"/>
                    <CheckBox x:Name="ChkStartMin" Content="Start minimized to tray"/>
                    <CheckBox x:Name="ChkNotify" Content="Toast when a game starts" IsChecked="True"/>
                    <CheckBox x:Name="ChkBackup" Content="Backup config before save" IsChecked="True"/>
                    <CheckBox x:Name="ChkTrayClose" Content="Close window → stay in tray" IsChecked="True"/>

                    <TextBlock x:Name="LblTips" Margin="0,16,0,0" FontSize="12" TextWrapping="Wrap"
                               Foreground="{DynamicResource MutedBrush}"
                               Text="After setup: use the Play tab for live color while gaming, Presets for hotkey profiles, and Pack gallery for ready-made packs."/>
                </StackPanel>
            </ScrollViewer>

            <DockPanel Grid.Row="2">
                <Button x:Name="BtnFinish" Content="Finish" Style="{StaticResource AccentButton}"
                        HorizontalAlignment="Right" MinWidth="110" Click="Finish_Click"/>
            </DockPanel>
        </Grid>
    </Border>
</Window>
